var tagsSeparated = {
    splitString : function(){
        var comma = ',';
        var space = ' ';
        
        var arrayOfStrings = arrayOfStrings.forEach(function(stringToSplit, separator ){
            
            return stringToSplit.split(separator);
        });
        
        
    }
};
